"use strict";
const webExHostedWebsite = require("../../common").webExHostedWebsite;

module.exports = TrainingCenter;

function TrainingCenter(yourWebExHostedName) {
    webExHostedWebsite.apply(this, arguments);
    // set commands available to Training Center
}