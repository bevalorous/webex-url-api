"use strict";
const webExHostedWebsite = require("../../common").webExHostedWebsite;

module.exports = SalesCenter;

function SalesCenter(yourWebExHostedName) {
    webExHostedWebsite.apply(this, arguments);
    // set commands available to Sales Center
}